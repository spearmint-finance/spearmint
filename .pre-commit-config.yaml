# Pre-commit hooks configuration
# Install: pip install pre-commit
# Setup: pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # Comprehensive pre-commit test suite with detailed logging
  - repo: local
    hooks:
      - id: precommit-test-suite
        name: Pre-Commit Test Suite (with logging)
        entry: python dev-tools/run_precommit_tests.py
        language: system
        files: ^(core-api/src/financial_analysis/api/.*\.py|sdk/openapi\.json)$
        stages: [commit, push]
        pass_filenames: false
        verbose: true

  # Individual hooks (can be run separately if needed)
  # OpenAPI generation hook
  - repo: local
    hooks:
      - id: openapi-generation
        name: OpenAPI Generation
        entry: python core-api/scripts/api_validation/api_validation.py --generate --output sdk/openapi.json
        language: system
        files: ^core-api/src/financial_analysis/api/.*\.py$
        stages: [manual]
        pass_filenames: false
        verbose: true

  # OpenAPI spec validation hook (Postman governance rules)
  - repo: local
    hooks:
      - id: openapi-spec-validation
        name: OpenAPI Spec Validation (Postman)
        entry: python dev-tools/validate_spec.py --spec-file sdk/openapi.json --fail-severity WARNING
        language: system
        files: ^sdk/openapi\.json$
        stages: [manual]
        pass_filenames: false
        verbose: true
