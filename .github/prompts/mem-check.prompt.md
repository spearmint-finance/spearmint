---
description: Search the memory trails for knowledge relevant to the current task.
model: gpt-4o
---
Look at the code currently selected or the file open in the editor.
Formulate a search query to check our `mx memories` database for past decisions or similar patterns.

1. Identify the core intent of the current code (e.g., "Implementing Auth", "Fixing Race Condition").
2. Generate an `mx memories search` command (or equivalent read command) with queries relevant to this intent and execute it.
3. Explain briefly *why* you are checking these specific topics.